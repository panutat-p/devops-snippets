# Files

## tar

Compress
```shell
tar -cvf archive.tar directory
```

Compress using gzip
```shell
tar -cvzf archive.tar.gz directory
```

Extract
```shell
tar -xvf archive.tar
```

Extract gzipped tar
```shell
tar -xvzf archive.tar.gz
```
